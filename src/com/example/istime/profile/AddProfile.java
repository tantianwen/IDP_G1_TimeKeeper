package com.example.istime.profile;

import android.app.Activity;

public class AddProfile extends Activity {

	//@Override
	/*protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_add_profile);
		
		Button add_category_cancel= (Button) findViewById(R.id.add_category_cancel);
		add_category_cancel.setOnClickListener(new View.OnClickListener() {
		    @Override
		    public void onClick(View v) {
		    	Intent i = new Intent(AddProfile.this, Profile1.class);
		    	 startActivity(i);
		    }
		});
		
		Button add_category_confirm= (Button) findViewById(R.id.add_category_confirm);
		add_category_confirm.setOnClickListener(new View.OnClickListener() {
		    @Override
		    public void onClick(View v) {
		    	Intent i = new Intent(AddProfile.this, Profile1.class);
		    	startActivity(i);
		    }
		});
	}

	@Override
	public boolean onCreateOptionsMenu(Menu menu) {
		// Inflate the menu; this adds items to the action bar if it is present.
		getMenuInflater().inflate(R.menu.add_profile, menu);
		return true;
	}*/
	

	
}
